spring.application.name=loans-service

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/libraryflow_loans_db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JPA configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Server configuration
server.port=8084

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.libraryflow=DEBUG
logging.file.name=logs/loans-service.log

# Web client configuration
services.users.url=http://localhost:8081
services.books.url=http://localhost:8083